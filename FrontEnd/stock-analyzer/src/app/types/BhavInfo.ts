export type BhavInfo = {
    companySymbol: string,
    series: string,
    date: Date,
    previousClose: Number,
    openPrice: Number,
    highPrice: Number,
    lowPrice: Number,
    lastPrice: Number,
    closePrice: Number,
    avgPrice: Number,
    ttlTrdQnty: Number,
    turnOverLacs: Number,
    noOfTrades: Number,
    deliveryQty: Number,
    deliveryPercentage: Number
}